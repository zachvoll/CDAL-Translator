#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <math.h>
//#include "Matrix.h"
using namespace std;

int main() {
int rows;
 rows = (y + 100) * x + f(33) + ({int y;
 y = 3;
 y + x;});
 int cols;
 cols = 50 + y +  + (!(x > 4))? (6): ( y + 3 );
 
return 0;
}
